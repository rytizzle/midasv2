.DS_Store
node_modules/
# Anchored to repo root so these do NOT match the committed .deploy/ payload
# (.deploy/dist, .deploy/client/dist) — Option 1 ships prebuilt artifacts.
/client/dist/
/dist/
/build/
.env
.databricks/
.smoke-test/
test-results/
playwright-report/

# Auto-generated types (endpoint-specific, varies per developer)
shared/appkit-types/serving.d.ts
